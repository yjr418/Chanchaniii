# 로컬 개발 — Google Cloud TTS 서비스 계정 JSON 파일 경로
GOOGLE_APPLICATION_CREDENTIALS=./gcp-tts-key.json

# 클라우드 배포(Render 등) — 서비스 계정 JSON 전체를 한 줄로 붙여넣기
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# TTS API 서버 포트 (로컬 개발 기본 3001, 배포 시 PORT 자동 사용)
TTS_PORT=3001
